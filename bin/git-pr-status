#!/bin/sh

if [ -f .prinfo ]; then
    
    # read PR URL   
    source .prinfo

    # call rest api
    STATE=`curl $PR_API_URL | jq '.state'`
    MERGED=`curl $PR_API_URL | jq '.merged'`

    echo "Status: $STATE"
    echo "Merged: $MERGED"
    
else
    echo "No .prinfo file found"
fi